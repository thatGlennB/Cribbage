using Cribbage.Model.CombinationTree;
using Cribbage.Model;

namespace Cribbage.Test
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
            //RootNode selection = new(new(Enumerable.Empty<Card>().ToHashSet(), Enumerable.Empty<Card>().ToHashSet()));
        }
    }
}